---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: certuploads.aliyun.weyundong.com
spec:
  group: aliyun.weyundong.com
  names:
    kind: CertUpload
    listKind: CertUploadList
    plural: certuploads
    shortNames:
    - cu
    - cupload
    singular: certupload
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.lastSyncTime
      name: Last Sync
      type: date
    - jsonPath: .spec.domain
      name: Domain
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CertUpload is the Schema for the certuploads API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CertUploadSpec defines the desired state of CertUpload
            properties:
              accessKeyIdRef:
                description: |-
                  AccessKeyIDRef is a reference to a Secret containing the Aliyun Access Key ID.
                  The Secret must contain a key named "accessKeyId".
                properties:
                  key:
                    description: Key in the Secret's data to use.
                    minLength: 1
                    type: string
                  name:
                    description: Name of the Secret.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the Secret. If empty, defaults to the
                      namespace of the CertUpload resource.
                    type: string
                required:
                - key
                - name
                type: object
              accessKeySecretRef:
                description: |-
                  AccessKeySecretRef is a reference to a Secret containing the Aliyun Access Key Secret.
                  The Secret must contain a key named "accessKeySecret".
                properties:
                  key:
                    description: Key in the Secret's data to use.
                    minLength: 1
                    type: string
                  name:
                    description: Name of the Secret.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the Secret. If empty, defaults to the
                      namespace of the CertUpload resource.
                    type: string
                required:
                - key
                - name
                type: object
              bucket:
                description: Bucket is the name of the OSS bucket where the domain
                  certificate should be updated.
                minLength: 1
                type: string
              certManagerCertRef:
                description: CertManagerCertRef is a reference to the cert-manager
                  Certificate resource.
                properties:
                  name:
                    description: Name of the Certificate resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the Certificate resource. If empty,
                      defaults to the namespace of the CertUpload resource.
                    type: string
                required:
                - name
                type: object
              domain:
                description: Domain is the domain name for which the certificate should
                  be uploaded.
                minLength: 1
                type: string
              region:
                description: Region is the Aliyun region where the SSL certificate
                  service and OSS bucket are located.
                minLength: 1
                type: string
            required:
            - accessKeyIdRef
            - accessKeySecretRef
            - bucket
            - certManagerCertRef
            - domain
            - region
            type: object
          status:
            description: CertUploadStatus defines the observed state of CertUpload
            properties:
              certificateFingerprint:
                description: CertificateFingerprint is the fingerprint (e.g., SHA256)
                  of the last uploaded certificate.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the CertUpload's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              errorMessage:
                description: ErrorMessage contains a human-readable error message
                  if the last sync failed.
                type: string
              lastSyncTime:
                description: LastSyncTime is the timestamp when the certificate was
                  last successfully synchronized to Aliyun.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
