# SECURITY WARNING: This sample contains placeholder secret values.
# DO NOT use these values in production or commit them to version control.
# Always use proper secret management for sensitive credentials.
# Recommended approaches:
# - External Secrets Operator
# - HashiCorp Vault
# - Kubernetes Secrets with encryption at rest
# - Sealed Secrets
---
apiVersion: v1
kind: Secret
metadata:
  name: aliyun-credentials
  namespace: default
type: Opaque
stringData:
  accessKeyId: "your-access-key-id"
  accessKeySecret: "your-access-key-secret"
  # Note: In production, consider using more secure methods like:
  # - External Secrets Operator
  # - Kubernetes Secrets with encryption at rest
  # - HashiCorp Vault integration